 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wechat.dao.onecommentDao">
	<select id="getonecomment" resultType="java.util.Map">
		select * from onecomment
	</select>
		<insert id="inonecomment">
		insert into
		onecomment (onecomment) VALUES
		(#{onecomment})
	</insert>
</mapper>